# FurTwin 发行前回归测试清单

本文档用于版本发布前的回归测试，请按顺序逐项检查。

---

## 一、开发版测试（`npm run dev`）

### 1.1 启动与退出

| 项目 | 说明 |
|------|------|
| **操作步骤** | 1. 执行 `npm run dev`；2. 等待宠物窗口出现；3. 右键宠物 → 退出 |
| **通过标准** | 宠物正常显示，退出后进程完全结束，无残留 |

### 1.2 系统托盘图标与菜单

| 项目 | 说明 |
|------|------|
| **操作步骤** | 1. 检查系统托盘区域是否出现 FurTwin 图标；2. 左键点击托盘图标；3. 右键点击托盘图标 |
| **通过标准** | 托盘图标正常显示；左键打开控制面板；右键弹出菜单（显示控制面板、切换动作、隐身模式、开机自启、重新加载动画、退出） |

### 1.3 控制面板

| 项目 | 说明 |
|------|------|
| **操作步骤** | 1. 通过托盘图标或宠物右键菜单打开控制面板；2. 检查各 Tab 是否正常切换 |
| **通过标准** | 控制面板正常打开，各 Tab（行为与模式、外观、设置等）可正常切换 |

### 1.4 动作库

| 项目 | 说明 |
|------|------|
| **操作步骤** | 1. 打开控制面板 → 行为与模式；2. 查看动作库列表；3. 点击某个动作进行切换 |
| **通过标准** | 动作库正常显示所有已导入动作，点击后宠物切换到对应动作 |

### 1.5 批量导入动作包

| 项目 | 说明 |
|------|------|
| **操作步骤** | 1. 准备一个包含多个动作的 zip 包；2. 通过控制面板导入 |
| **通过标准** | 导入成功，动作库中出现新导入的动作 |

### 1.6 导出动作包

| 项目 | 说明 |
|------|------|
| **操作步骤** | 1. 选择一个或多个动作；2. 执行导出操作 |
| **通过标准** | 导出成功，生成的 zip 包内容完整 |

### 1.7 宠物位置记忆

| 项目 | 说明 |
|------|------|
| **操作步骤** | 1. 拖动宠物到屏幕某个位置；2. 退出应用；3. 重新启动应用 |
| **通过标准** | 宠物出现在上次退出时的位置 |

### 1.8 隐身模式与鼠标穿透

| 项目 | 说明 |
|------|------|
| **操作步骤** | 1. 通过托盘菜单或控制面板开启隐身模式；2. 尝试点击宠物所在区域 |
| **通过标准** | 隐身模式开启后宠物不可见；鼠标穿透生效，点击事件穿透到下层窗口 |

### 1.9 开机自启开关

| 项目 | 说明 |
|------|------|
| **操作步骤** | 1. 在控制面板或托盘菜单中开启开机自启；2. 检查系统启动项；3. 关闭开机自启；4. 再次检查 |
| **通过标准** | 开启后系统启动项中有 FurTwin；关闭后启动项被移除 |

### 1.10 自动行为

| 项目 | 说明 |
|------|------|
| **操作步骤** | 1. 开启自动行为；2. 等待一段时间观察宠物是否自动切换动作 |
| **通过标准** | 宠物按配置的轮数自动切换动作 |

### 1.11 恢复内置预览

| 项目 | 说明 |
|------|------|
| **操作步骤** | 1. 在设置中找到恢复内置预览选项；2. 执行恢复 |
| **通过标准** | 内置预览动作被正确恢复 |

### 1.12 动作资源丢失 Fallback

| 项目 | 说明 |
|------|------|
| **操作步骤** | 1. 手动删除当前动作的资源文件；2. 观察宠物表现 |
| **通过标准** | 宠物自动 fallback 到默认动作，不崩溃 |

---

## 二、打包版测试

### 打包命令

| 命令 | 产物 |
|------|------|
| `npm run dist` | 安装版 `FurTwin-版本号-setup.exe` + 免安装版 `FurTwin-版本号-win-unpacked.zip` |
| `npm run dist:win` | 仅安装版 |
| `npm run dist:win:zip` | 仅免安装版 |

产物路径：`dist/` 目录下

---

### A. 安装版测试（setup.exe）

#### A.1 打包产物检查

| 项目 | 说明 |
|------|------|
| **操作步骤** | 1. 执行 `npm run dist`；2. 检查 `dist/` 目录下是否生成 `FurTwin-版本号-setup.exe` |
| **通过标准** | 打包成功，setup.exe 存在 |

#### A.2 安装向导

| 项目 | 说明 |
|------|------|
| **操作步骤** | 1. 双击 `FurTwin-版本号-setup.exe`；2. 观察是否出现安装向导界面 |
| **通过标准** | 显示安装向导（非静默安装），有"下一步"等按钮 |

#### A.3 选择安装目录

| 项目 | 说明 |
|------|------|
| **操作步骤** | 1. 在安装向导中找到目录选择步骤；2. 修改安装目录 |
| **通过标准** | 可以自定义安装目录，非固定路径 |

#### A.4 安装后启动

| 项目 | 说明 |
|------|------|
| **操作步骤** | 1. 完成安装；2. 等待 FurTwin 启动 |
| **通过标准** | FurTwin 正常启动，宠物窗口出现 |

#### A.5 安装版功能验证

| 项目 | 说明 |
|------|------|
| **操作步骤** | 依次测试：Tray 图标、控制面板、隐身模式、动作切换、自动行为、退出 |
| **通过标准** | 各项功能正常 |

#### A.6 卸载

| 项目 | 说明 |
|------|------|
| **操作步骤** | 1. 通过系统设置或控制面板卸载 FurTwin；2. 确认卸载完成 |
| **通过标准** | 卸载正常，无残留进程，开始菜单快捷方式被移除 |

---

### B. 免安装版测试（zip）

#### B.1 打包产物检查（v0.1.4 预期资产）

| 项目 | 说明 |
|------|------|
| **操作步骤** | 1. 执行 `npm run dist`；2. 检查 `dist/` 目录下是否生成预期资产 |
| **预期资产名称** | `furtwin-0.1.4-setup.exe`（安装版）、`furtwin-0.1.4-win-unpacked.zip`（免安装版）|
| **通过标准** | 打包成功，两个预期资产文件均存在 |
| **当前状态** | ✅ 已通过（安装版 + 免安装版均已生成并人工验证）|

#### B.2 解压与运行

| 项目 | 说明 |
|------|------|
| **操作步骤** | 1. 解压 `furtwin-0.1.4-win-unpacked.zip` 到任意目录（含中文和空格的目录）；2. 运行 `furtwin.exe` |
| **通过标准** | 解压成功，furtwin.exe 可正常运行 |

#### B.3 免安装版功能验证

| 项目 | 说明 |
|------|------|
| **操作步骤** | 依次测试：Tray 图标、控制面板、隐身模式、动作切换、自动行为、退出 |
| **通过标准** | 各项功能正常，与安装版表现一致 |

---

## 三、最终确认

- [x] 开发版全部通过
- [x] 打包版全部通过
- [x] 无控制台报错
- [x] 无残留进程
- [x] P7-RC0 组合回归测试通过（58 通过 / 1 部分 / 0 未测）

### 发布态说明（不作为复选项目，仅记录当前状态）

- **功能冻结 Commit**：`c57d946`（fix: 修复安装路径含空格时视频提取失败）
- **功能冻结 Commit 链**：`b901d35` → `c261812` → `7368429` → `2bb7009` → `e79486d` → `c57d946`
- **最终发布 Commit**：以 `v0.1.4` tag 指向为准
- master 尚未合并 dev（发布收口时 fast-forward）
- v0.1.4 tag 尚未打
- GitHub Release 尚未创建
- 发布前确认 `master`、`dev` 与 `v0.1.4` tag 指向同一最终 commit

---

## 四、P7-RC0 测试结果摘要（2026-07-07）

| 阶段 | 项数 | 结果 |
|------|------|------|
| A. 启动 / 退出 | 6 | ✅ 全部通过 |
| B. 桌宠显示 / 透明 / 拖拽 / 右键 | 6 | ✅ 全部通过 |
| C. Tray 菜单 / 找回桌宠 | 8 | ✅ 全部通过 |
| D. 控制面板 | 3 | ✅ 全部通过 |
| E. 动作库 | 6 | ✅ 全部通过 |
| F. 自动行为 | 3 | ✅ 全部通过 |
| G. local.config.json | 6 | ✅ 5 项通过，1 项部分（G3/G4 未深度破坏实测）|
| H. 主进程日志 | 7 | ✅ 全部通过 |
| I. 控制面板 ErrorBoundary | 5 | ✅ 全部通过 |
| J. 桌宠 ErrorBoundary | 5 | ✅ 全部通过 |
| K. Git / 产物 | 4 | ✅ 全部通过 |
| **合计** | **59** | **58 通过 / 1 部分 / 0 未测** |

### G3/G4 部分说明

- **测试点**：`.bak` 恢复 / 双坏 fallback
- **本轮结果**：未深度破坏实测
- **原因**：该测试需要故意破坏 `local.config.json` 并重启开发进程，存在人工误操作成本，未在 RC 阶段执行
- **依据**：P7A-1 单项阶段已直接验证配置损坏恢复逻辑；RC 阶段通过正常保存、日志、tmp 清理、代码路径复核确认无退化
- **建议**：发布前如有余力可选做 1 次 G3/G4 深度复测，但不作为 v0.1.3 发布的阻塞项

### 当前开发态状态

- 版本号：已升级到 v0.1.3（package.json / package-lock.json）
- git merge master：**未执行**
- v0.1.3 tag：**未打**
- 正式安装资产（furtwin-0.1.3-setup.exe / furtwin-0.1.3-win-unpacked.zip）：**未生成**
- GitHub Release v0.1.3：**未创建**

### 内部备注：EPIPE 说明

> 开发期使用 `npm run dev | head` 等管道截断命令启动 Electron 时，`head` 提前关闭管道会导致主进程输出 `EPIPE: broken pipe` 错误。这是开发命令使用问题，非产品回归，不写入公开 Release Notes 或 USER_GUIDE。

---

## 五、v0.1.4 专项验证（2026-07-10）

以下项目在 v0.1.4 发布前完成人工验证：

### 5.1 安装版

| 项目 | 说明 | 状态 |
|------|------|------|
| **所有用户默认路径** | 安装到 `C:\Program Files\furtwin`，使用含中文路径视频执行完整透明帧提取 | ✅ 通过 |
| **仅当前用户安装** | 安装到 `%LOCALAPPDATA%\Programs\FurTwin` | ✅ 通过 |
| **FFmpeg 版本检测** | 安装版启动后可正常显示 FFmpeg 版本信息 | ✅ 通过 |
| **ffprobe 分辨率探测** | 安装路径含空格时仍能正确获取视频分辨率 | ✅ 通过 |
| **安装路径空格修复验证** | `C:\Program Files\furtwin` 不再触发「无法获取视频分辨率」| ✅ 通过 |

### 5.2 免安装版

| 项目 | 说明 | 状态 |
|------|------|------|
| **含中文和空格目录** | 解压到含中文和空格的仓库外目录后完整运行 | ✅ 通过 |
| **免安装版功能** | 桌宠、托盘、动作、自动行为、诊断入口、退出 | ✅ 通过 |

### 5.3 v0.1.4 新增功能

| 功能 | 说明 | 状态 |
|------|------|------|
| **退出入口** | 控制面板标题栏「退出 FurTwin」按钮 | ✅ 通过 |
| **打开日志目录** | 行为与模式 → 诊断 → 打开日志目录 | ✅ 通过 |
| **打开配置目录** | 行为与模式 → 诊断 → 打开配置目录 | ✅ 通过 |
| **查看最近日志** | 行为与模式 → 诊断 → 查看最近日志（含刷新/收起）| ✅ 通过 |
| **preload 调试输出清理** | 提取流程不再遗留 console.log 调试输出 | ✅ 通过 |

### 5.4 退出链完整性

| 检查项 | 状态 |
|------|------|
| 退出后无残留进程 | ✅ |
| 退出无 EPIPE | ✅ |
| 退出无 uncaughtException | ✅ |
| 退出无 unhandledRejection | ✅ |
| userData 保留（配置/动作/排序）| ✅ |

---

*最后更新：2026-07-10*
